<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Prepace" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Prepace" parent="Base.Theme.Prepace" />

    <style name="Theme.App.Splash" parent="Theme.Prepace">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <!-- Optional: Status bar color if needed to match -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>